class Program {
	void main() {
            int x; int y;
            x = 10;
            y = 5;
            print(gcd(x, y));
         }
    int gcd(int a, int b) {
        if ( b == 0 )
            return a;
        else
            return gcd(b, a % b);
    }
}